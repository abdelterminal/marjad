# TCK-007 — Homepage hero section

**Owner:** Frontend
**Depends on:** TCK-006
**Priority:** P0

## Goal
Build the homepage hero with editorial headline, tagline and CTA into products.

## Acceptance criteria
- [ ] Hero with display-font headline, supporting line, primary CTA → `/[locale]/products`
- [ ] Placeholder hero image optimized via `next/image` with `priority` (LCP)
- [ ] Responsive + RTL-correct (text alignment, CTA position)
- [ ] Strings from message files
- [ ] No CLS from hero image (explicit dimensions / aspect ratio)

## Technical notes
Server Component. This is the LCP element — only this image gets `priority`. Keep copy editorial per `01-brand.md` voice (avoid superlatives). Image is a placeholder asset until brand delivers.
